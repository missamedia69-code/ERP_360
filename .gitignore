# Fichiers générés par Gradle / Android Studio
*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build/
/build/

# Sorties de build
*.apk
*.aab
*.ap_
*.dex

# Keystore (ne jamais commiter de signatures)
*.jks
*.keystore

# Vidéos de travail (sources d'intro) — seules celles de res/raw sont suivies
/*.mp4

# Logs
*.log
/build.log
/tests.log

# Dépendances et résidus Node/npm (sans rapport avec l'app Android)
/node_modules/
/package.json
/package-lock.json

# Outillage local (génération d'icônes, scripts d'audit) — hors dépôt par défaut
/tools/
